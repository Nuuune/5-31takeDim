@import "components/block.wxss";
@import "components/banners.wxss";
@import "components/auctions.wxss";
@import "components/article-products.wxss";
@import "components/article.wxss";

.container {
  background-color: #fff;
} 

.scroll-view {
  height: calc(100%);
}

.loading{
    width: 80px;
    height: 40rpx;
    margin: 0 auto;
    margin-top:100rpx;
}
.loading span{
    display: inline-block;
    width: 8px;
    height: 100%;
    border-radius: 4px;
    background: lightgreen;
    animation: load 1s ease infinite;
}
@keyframes load {
    0%,100%{
        height: 40rpx;
        background: lightgreen;
    }
    50%{
        height: 70rpx;
        margin: -15rpx 0;
        background: lightgreen;
    }
}
.loading span:nth-child(2){
    animation-delay:0.2s;
}
.loading span:nth-child(3){
    animation-delay:0.4s;
}
.loading span:nth-child(4){
    animation-delay:0.6s;
}
.loading span:nth-child(5){
    animation-delay:0.8s;
}

.search {
  padding: 48rpx 36rpx;
  font-size: 16px;
  color: rgba(0, 0, 0, .2);
  line-height: 1;
}

.search .fa {
  float: right;
  color: rgba(0, 0, 0, .6);
}

.block.articleTitle {
  padding-bottom: 0;
}